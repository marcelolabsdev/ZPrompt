[project]
name = "zprompt"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
]

[tool.vercel]
entrypoint = "main:app"
