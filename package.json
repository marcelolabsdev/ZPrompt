{
  "name": "zprompt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i src/input.css -o public/css/styles.css --minify",
    "dev:css": "npx @tailwindcss/cli -i src/input.css -o public/css/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
